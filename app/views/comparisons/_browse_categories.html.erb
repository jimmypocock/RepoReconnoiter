<!-- Browse by Category Section -->
<div class="mb-12">
  <h2 class="text-2xl font-bold text-gray-900 mb-6">Browse by Category</h2>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    <!-- Problem Domains -->
    <div>
      <h3 class="text-lg font-semibold text-gray-900 mb-4 flex items-center gap-2">
        <span class="text-xl">🎯</span>
        Problem Domains
      </h3>
      <div class="space-y-3">
        <% @homepage.top_problem_domains.each do |category| %>
          <div class="bg-white rounded-xl shadow border border-gray-200 p-4 hover:shadow-md transition-all">
            <div class="flex items-center justify-between mb-2">
              <h4 class="font-semibold text-gray-900 text-sm"><%= category.name %></h4>
              <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                <%= category.repositories_count %> repos
              </span>
            </div>
            <!-- Sample repos -->
            <% category.repositories.limit(2).each do |repo| %>
              <div class="text-xs text-gray-600 flex items-center gap-1 mt-1">
                <span>⭐</span>
                <span class="font-medium"><%= repo.full_name.split('/').last %></span>
                <% if repo.stargazers_count && repo.stargazers_count > 0 %>
                  <span class="text-gray-400">(<%= number_to_human(repo.stargazers_count, precision: 1) %>★)</span>
                <% end %>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Architecture Patterns -->
    <div>
      <h3 class="text-lg font-semibold text-gray-900 mb-4 flex items-center gap-2">
        <span class="text-xl">🏗️</span>
        Architecture Patterns
      </h3>
      <div class="space-y-3">
        <% @homepage.top_architecture_patterns.each do |category| %>
          <div class="bg-white rounded-xl shadow border border-gray-200 p-4 hover:shadow-md transition-all">
            <div class="flex items-center justify-between mb-2">
              <h4 class="font-semibold text-gray-900 text-sm"><%= category.name %></h4>
              <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-purple-100 text-purple-800">
                <%= category.repositories_count %> repos
              </span>
            </div>
            <!-- Sample repos -->
            <% category.repositories.limit(2).each do |repo| %>
              <div class="text-xs text-gray-600 flex items-center gap-1 mt-1">
                <span>⭐</span>
                <span class="font-medium"><%= repo.full_name.split('/').last %></span>
                <% if repo.stargazers_count && repo.stargazers_count > 0 %>
                  <span class="text-gray-400">(<%= number_to_human(repo.stargazers_count, precision: 1) %>★)</span>
                <% end %>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Maturity Levels -->
    <div>
      <h3 class="text-lg font-semibold text-gray-900 mb-4 flex items-center gap-2">
        <span class="text-xl">📊</span>
        Maturity Levels
      </h3>
      <div class="space-y-3">
        <% @homepage.top_maturity_levels.each do |category| %>
          <div class="bg-white rounded-xl shadow border border-gray-200 p-4 hover:shadow-md transition-all">
            <div class="flex items-center justify-between mb-2">
              <h4 class="font-semibold text-gray-900 text-sm"><%= category.name %></h4>
              <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                <%= category.repositories_count %> repos
              </span>
            </div>
            <!-- Sample repos -->
            <% category.repositories.limit(2).each do |repo| %>
              <div class="text-xs text-gray-600 flex items-center gap-1 mt-1">
                <span>⭐</span>
                <span class="font-medium"><%= repo.full_name.split('/').last %></span>
                <% if repo.stargazers_count && repo.stargazers_count > 0 %>
                  <span class="text-gray-400">(<%= number_to_human(repo.stargazers_count, precision: 1) %>★)</span>
                <% end %>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
