<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Repo Reconnoiter" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Repo Reconnoiter">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      <!-- Navigation Bar for Signed-In Users -->
      <nav class="bg-white border-b border-gray-200">
        <div class="container mx-auto px-4">
          <div class="flex items-center justify-between h-16">
            <!-- Logo/Home Link -->
            <%= link_to root_path, class: "text-lg font-semibold text-gray-900 hover:text-gray-700" do %>
              RepoReconnoiter
            <% end %>

            <!-- User Menu -->
            <div class="flex items-center gap-4">
              <div class="flex items-center gap-2">
                <% if current_user.github_avatar_url.present? %>
                  <%= image_tag current_user.github_avatar_url, class: "w-8 h-8 rounded-full", alt: current_user.github_username %>
                <% end %>
                <span class="text-sm text-gray-700"><%= current_user.github_username %></span>
              </div>
              <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "text-sm text-gray-600 hover:text-gray-900" %>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <!-- Flash Messages -->
    <% if flash[:notice] || flash[:alert] %>
      <div class="container mx-auto px-4 mt-4">
        <% if flash[:notice] %>
          <div class="p-4 bg-blue-50 border border-blue-200 rounded-lg text-blue-800 mb-4">
            <%= flash[:notice] %>
          </div>
        <% end %>
        <% if flash[:alert] %>
          <div class="p-4 bg-red-50 border border-red-200 rounded-lg text-red-800 mb-4">
            <%= flash[:alert] %>
          </div>
        <% end %>
      </div>
    <% end %>

    <main class="container mx-auto flex justify-center">
      <%= yield %>
    </main>
  </body>
</html>
